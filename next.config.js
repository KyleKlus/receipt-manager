/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
  reactStrictMode: true,
  basePath: '/receipt-manager',
  env: {
    basePath: '/receipt-manager',
  },
};

module.exports = nextConfig;
